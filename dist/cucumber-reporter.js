"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const path = require("path");
const reporter = require('cucumber-html-reporter');
class CucumberReporter {
    constructor() {
        this._outputPath = path.resolve(__dirname, '../cucumber-output');
        this._options = {
            theme: 'bootstrap',
            jsonDir: this.outputPath,
            output: path.resolve(this.outputPath, 'cucumber_report.html'),
            reportSuiteAsScenarios: true,
            launchReport: true,
            metadata: {
                "Generated By:": "Cukes-JS"
            }
        };
    }
    generate() {
        reporter.generate(this.options);
    }
    get outputPath() {
        return this._outputPath;
    }
    set outputPath(value) {
        this._outputPath = value;
    }
    get options() {
        return this._options;
    }
    set options(value) {
        this._options = value;
    }
}
exports.CucumberReporter = CucumberReporter;
